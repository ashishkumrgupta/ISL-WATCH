---
description: ASL video overlay project roadmap and milestone order
alwaysApply: true
---

# ASL Watch — development context

Read [docs/ROADMAP.md](../docs/ROADMAP.md) for the full 5-milestone plan.

**Milestones 1–3 done.** **Next: Milestone 4** — rigged 3D avatar in `web/` (replace canvas silhouette in `GlossCanvas.tsx`).

- Backend: `backend/` — repositories → services → providers → api/routes
- Frontend: `web/` — React + YouTube IFrame + gloss canvas; see [docs/WEB_PLAYER.md](../docs/WEB_PLAYER.md)
- Swap LLM in `providers/gloss/`. Auth hook in `api/deps.py`.
